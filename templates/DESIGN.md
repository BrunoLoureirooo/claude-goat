# DESIGN.md

Canonical visual system for this project. `CLAUDE.md` carries the short non-negotiables; this file is the source of truth for anything visual. `/impeccable` reads this.

<!-- INIT: fill in during project init. If the project has no UI at all, replace this file's
     entire contents with one line saying so, and delete the Design rules section from CLAUDE.md. -->

## Tokens

<!-- INIT: the CSS custom properties / design tokens and where they live (e.g. src/styles/tokens.css).
     List the core ones with their values and their job: background, foreground, hero color,
     accent, semantic states. State the accent budget explicitly ("green is the hero, amber is
     the ONE sparing accent, no third hue"). -->

## Typography

<!-- INIT: font families and weights, where each is used, and the scale. If it's a single font
     everywhere, say so — that's a rule, not an accident. -->

## Color

<!-- INIT: full palette with hex values, contrast requirements, dark/light behavior. Which
     colors may glow/saturate and which never do (e.g. "glow on bright headings only, never body"). -->

## Spacing & layout

<!-- INIT: spacing scale, max content width, grid/breakpoint rules, and the overall shell
     metaphor if there is one (e.g. "whole page = one terminal window"). -->

## Motion

<!-- INIT: what moves and why — durations, easings, signature animations. Every animation must
     have a prefers-reduced-motion fallback; state what each one degrades to. -->

## Accessibility

<!-- INIT: contrast floor, focus-visible treatment, keyboard expectations, reduced-motion
     summary. The floor, not the ceiling. -->
